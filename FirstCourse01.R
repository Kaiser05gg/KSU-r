############################################################
# FirstCourse01_all_code_with_comments.R
# データ・AI活用実践 第1回
# ipynb → R スクリプト変換（全コード＋全コメント付き）
############################################################


############################################################
# Section 1: R の基本動作
############################################################

version  # R のバージョンを表示

# 基本的な四則演算
1 + 5     # 足し算
3 - 10    # 引き算
4 * 5     # 掛け算
6 / 2     # 割り算
7 / 2     # 割り算（端数が出る）

# 演算のバリエーション
4.0 * 5        # 浮動小数点計算
-5 * -4        # マイナス × マイナス（正）
3 - (-10)      # マイナスを引く → 足すと同じ

# 優先順位の違いを確認
1 + 2 * -4 - 6
(1 + 2) * -4 - 6
(1 + 2) * (-4 - 6)


############################################################
# Section 2: 数学関数
############################################################

2**10     # 累乗（2^10）
10%%3     # 余り（mod）
pi        # 円周率 π
sqrt(2)   # √2
exp(2)    # e^2
log(10)   # 自然対数 ln(10)


############################################################
# Section 3: ベクトル生成と表示
############################################################

x <- c(1,2,3,4,5)   # ベクトル x を作成
x                  # 中身を表示

y <- c(5,4,3,2,1)   # ベクトル y を作成（逆順）
y


############################################################
# Section 4: ベクトルの要素抽出
############################################################

x[2]        # 2番目の要素
x[1:3]      # 1〜3番目
x[c(1,5)]   # 1番目と5番目


############################################################
# Section 5: ベクトル同士の演算
############################################################

x + y       # 要素ごとの足し算
x - y       # 引き算
x * y       # 掛け算
x / y       # 割り算


############################################################
# Section 6: ベクトルに対する数学関数
############################################################

sqrt(x)       # x の各要素の平方根
log10(x + y)  # x+y の常用対数


############################################################
# Section 7: array（配列）
############################################################

t <- array(10, c(10))     # 要素がすべて10の配列（長さ10）
t

z <- array(0:359, c(360)) # 0〜359 の連続配列
z


############################################################
# Section 8: seq（等差数列）
############################################################

x <- seq(0, 359, by = 60)     # 0〜359 を60刻みで
x

x <- seq(0, 360, by = 60)     # 0〜360 を60刻みで
x

y <- seq(300, -300, length = 7)  # 300〜-300 を7分割
y

# ベクトル同士の演算
x + y
x * y
x / y

# 関数もそのまま適用できる
sqrt(x + y)
log10(x)


############################################################
# Section 9: 条件抽出
############################################################

u <- c(1, 3, 5, 7, 9)

u[u >= 5]   # 5以上の要素だけ取り出す


############################################################
# Section 10: 基本統計値
############################################################

mean(u)   # 平均
max(u)    # 最大
min(u)    # 最小


############################################################
# Section 11: データフレーム
############################################################

df <- data.frame(
  a = c(1, 2, 3),      # 1列目
  b = c(10, 20, 30)    # 2列目
)

df    # 表示


############################################################
# Section 12: 関数を作る（自作関数）
############################################################

myfunc <- function(x) {
  return(2 * x + 5)   # 2x + 5 を返す
}

myfunc(1)                # x = 1
myfunc(-10)              # x = -10
myfunc(75892783459072)   # 巨大な数
myfunc(y)                # ベクトルに適用


############################################################
# Section 13: 演習用（未完成の関数）
############################################################

# 摂氏 → 華氏（Celsius-to-Fahrenheit）
# 本来は f = c × 9/5 + 32 で計算する

C2F <- function(cdeg) {
  fdeg <- 0            # 初期値（空の処理）
  
  # ここに本当はコードを書く：
  # fdeg <- cdeg * 9/5 + 32
  
  return(fdeg)
}

############################################################
# FirstCourse01 の全コード＋全コメント 完成！
############################################################
